{{- if .Values.imagePrepull.enabled }}
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "crucible.fullname" . }}-image-prepull
  namespace: {{ .Values.gateway.namespace }}
  labels:
    {{- include "crucible.labels" . | nindent 4 }}
    app.kubernetes.io/component: image-prepull
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "crucible.name" . }}-image-prepull
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "crucible.name" . }}-image-prepull
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      {{- with .Values.imagePrepull.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.imagePrepull.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        {{- range $i, $image := .Values.imagePrepull.images }}
        - name: pull-{{ $i }}
          image: {{ $image }}
          command: ["sh", "-c", "exit 0"]
          resources:
            requests:
              cpu: "10m"
              memory: "16Mi"
            limits:
              cpu: "10m"
              memory: "16Mi"
        {{- end }}
      containers:
        - name: pause
          image: registry.k8s.io/pause:3.9
          resources:
            requests:
              cpu: "10m"
              memory: "16Mi"
            limits:
              cpu: "10m"
              memory: "16Mi"
{{- end }}
