Crucible gateway has been installed.

Cloud provider: {{ .Values.cloudProvider }}
Namespace:      {{ .Values.gateway.namespace }}
Replicas:       {{ .Values.replicaCount }}

Get the gateway URL:
  kubectl -n {{ .Values.gateway.namespace }} port-forward svc/{{ include "crucible.fullname" . }} {{ .Values.gateway.port }}:{{ .Values.gateway.port }}

Then visit: http://localhost:{{ .Values.gateway.port }}/healthz
