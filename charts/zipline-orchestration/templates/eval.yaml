apiVersion: apps/v1
kind: Deployment
metadata:
  name: zipline-eval
  namespace: {{ .Release.Namespace }}
  labels:
    app: eval
    {{- include "zipline-orchestration.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.platform.orchestration.eval.replicas }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: eval
  template:
    metadata:
      labels:
        app: eval
    spec:
      serviceAccountName: {{ .Values.platform.serviceAccount.name }}
      {{- with .Values.platform.imagePullSecrets }}
      imagePullSecrets:
      {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: eval
          image: "{{ .Values.platform.orchestration.eval.image }}:{{ .Values.platform.version }}"
          imagePullPolicy: Always
          env:
            - name: ORCHESTRATION_PORT
              value: "{{ .Values.platform.orchestration.eval.port }}"
            - name: DB_URL
              value: "jdbc:postgresql://{{ .Values.infra.database.host }}/{{ .Values.infra.database.name }}"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: password
            - name: CUSTOMER_ID
              value: {{ .Values.infra.global.customerName | quote }}
            - name: ARTIFACT_PREFIX
              value: {{ .Values.infra.global.artifactPrefix | quote }}
            - name: AWS_REGION
              value: "{{ .Values.infra.aws.region }}"
            - name: AWS_DEFAULT_REGION
              value: "{{ .Values.infra.aws.region }}"
            - name: KV_TABLE_PREFIX
              value: "{{ .Values.infra.aws.kvTablePrefix }}"
            {{- if .Values.infra.aws.databricksSpSecretArn }}
            - name: DATABRICKS_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: databricks-sp-credentials
                  key: client_id
            - name: DATABRICKS_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: databricks-sp-credentials
                  key: client_secret
            {{- end }}
            - name: AUTH_ENABLED
              value: "{{ .Values.platform.auth.enabled }}"
            - name: AUTH_JWKS_URL
              value: "{{ .Values.platform.auth.jwksUrl }}"
          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets-store"
              readOnly: true
          ports:
            - containerPort: {{ .Values.platform.orchestration.eval.port }}
          resources:
          {{- toYaml .Values.platform.orchestration.eval.resources | nindent 12 }}
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "db-secret-provider"
---
apiVersion: v1
kind: Service
metadata:
  name: eval-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zipline-orchestration.labels" . | nindent 4 }}
spec:
  selector:
    app: eval
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.platform.orchestration.eval.port }}
      protocol: TCP
  type: ClusterIP
