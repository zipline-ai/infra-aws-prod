apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eval-ingress
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zipline-orchestration.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.platform.ingress.eval.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.platform.ingress.eval.className }}
  rules:
    {{- if .Values.infra.domains.eval }}
    - host: {{ .Values.infra.domains.eval }}
      http:
    {{- else }}
    - http:
    {{- end }}
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: eval-service
                port:
                  number: 80
